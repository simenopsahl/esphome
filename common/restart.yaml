---
# Restart
#
# A simple button to restart the ESP device.
#
# https://esphome.io/components/button/restart.html
#
button:
  - platform: restart
    id: button_restart
    name: "Restart"
    device_class: "restart"
labels: [esp32]
---
# Base functions for uPesy Wroom
#

# Pins for this model
packages:
  pins: !include pins/upesy_wroom.yaml
  i2c: !include extension/internal_i2c.yaml

substitutions:
  device:
    model: "uPesy Wroom"
    rmt_symbols_block: 64
    framework:
      type: esp-idf
  extension:
    i2c:
      internal:
        frequency: 100kHz

esphome:
  project:
    name: "ESP32.UPesy WROOM"
    version: "1.0.0"

esp32:
# uPesy ESP-WROOM-32
#  board: upesy_wroom
  variant: esp32
  flash_size: 4MB
  cpu_frequency: 240MHz
  framework:
    type: ${device.framework.type}

status_led:
  pin: ${pin.light.led}
